services:
  - type: web
    name: roomlink
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: CLIENT_ORIGIN
        value: "*"
      - key: ROOM_TTL_MINUTES
        value: "360"
